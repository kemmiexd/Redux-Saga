const styles = () => ({
  wrapper: {
    position: 'fixed',
    width: '100%',
    height: '100%',
    background: 'rgba(0, 0, 0, .3)',
    top: 0,
    left: 0,
    zIndex: 1000
  },
  icon: {
    position: 'absolute',
    top: '50%',
    left: '50%',
    transform: 'translate(-50%, -50%)',
    width: 200
  }
});

export default styles;
